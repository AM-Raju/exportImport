import React from "react";

const ProfileHeading = ({ children }) => {
  return (
    <h2 className="text-6xl font-bold text-center text-neutral-bg ">
      {children}
    </h2>
  );
};

export default ProfileHeading;
